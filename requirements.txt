# Deployment (Streamlit Community Cloud) — intentionally minimal.
#
# The hosted app is streamlit_app.py, which only embeds the pre-built
# self-contained dashboard/underwriting_copilot_mvp.html. It does no
# modelling at runtime, so it does NOT need numpy/pandas/scikit-learn/
# reportlab/pdfplumber. Keeping this file tiny makes cloud builds fast and
# immune to missing wheels on newer Python versions.
#
# To run the pipeline, API, or tests, install one of:
#   pip install -r requirements-pipeline.txt   # pipeline + REST API
#   pip install -r requirements-dev.txt        # the above + pytest
streamlit>=1.36
