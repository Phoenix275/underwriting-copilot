# Full pipeline + REST API runtime.
# Compatible ranges rather than exact pins so the project installs on any
# supported Python (CI validates the newest resolution on 3.13).
numpy>=1.26,<3
pandas>=2.1,<3
scikit-learn>=1.4,<2
reportlab>=4,<6
pdfplumber>=0.11,<0.12
joblib>=1.3,<2
httpx>=0.27,<1
fastapi>=0.110,<1
uvicorn>=0.29,<1
